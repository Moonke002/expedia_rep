# Part 1 prompt record

Build the Expedia Rep Part 1 flow from the supplied visual references: create an Expedia-inspired “Choose stay” screen with hotel-name search, supported city/price filters, repeated stay cards, dates, nightly rate, calculated stay total, and a no-results message. Join `hotels.csv` and `trips.csv` on `hotel_id` in FastAPI, avoid fabricating fields absent from the CSVs, manually review the changes, and record browser verification.
